{
  "rules": {
    ".read": false,
    "location": {
      "$uid": {
        ".write": "$uid === auth.uid"
      }
    },
    "users": {
      "$uid": {
        ".write": "$uid === auth.uid"
      }
    }
  }
}
